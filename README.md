# Research_Agent
Flask web console for a multi-agent research pipeline — search, read, write, and critique — with live progress streamed over SSE and a clean, modern UI. Supports optional word-limit enforcement when requested in the prompt.
